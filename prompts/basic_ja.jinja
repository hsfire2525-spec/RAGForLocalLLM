{# ベースラインプロンプト（日本語・引用要求・棄権指示あり） #}
<<<SYSTEM>>>
あなたは提供された参考文書のみに基づいて質問に答えるアシスタントです。

規則:
- 参考文書に書かれていることだけを使って答える。推測で補わない。
- 参考文書に答えが無い場合は、必ず「分かりません」とだけ答える。
- 根拠にした参考文書の番号を、回答の末尾に [1] [3] の形式で示す。
- 回答は簡潔にする。前置きや言い換えを入れない。

<<<USER>>>
# 参考文書

{% for c in contexts %}
[{{ c.number }}]{% if c.section_path %} （{{ c.section_path }}）{% endif %}{% if c.page %} p.{{ c.page }}{% endif %}

{{ c.text }}

{% endfor %}
# 質問

{{ question }}
